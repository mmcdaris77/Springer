version: '0.1'

rule_sets:
  - name: test
    gap_rules: 
      - name: gap_rule_1
        conditions:
          - is_mono_therapy:
          - new_drugs_contains:   # allow for a massive gap if regimen is mono and adding any of the drugs
              drugs: 
                - '40213227'
                - '1111111'
          - within_allowable_gap: 
              allowable_gap: 5000
        actions:
          - add_drugs_to_lot:
      - name: gap_rule_2
        conditions:
          - new_drugs_contains: 
              drugs: 
                - '40213228'
          - within_allowable_gap: 
              allowable_gap: 60
        actions:
          - do_nothing:

    addition_rules:
      - name: add_rule_1
        conditions:
          - new_drugs_contains: 
              drugs: 
                - '40213227'
          - within_allowable_gap: 
              allowable_gap: 180
        actions:
          - add_drugs_to_lot:
   
  - name: rule_config_test_2
    gap_rules: 
      - name: gap_rule_1
        conditions:
          - is_mono_therapy:
          - new_drugs_contains: 
              drugs: 
                - '40213227'
                - '1111111'
          - within_allowable_gap: 
              allowable_gap: 5000
        actions:
          - add_drugs_to_lot: